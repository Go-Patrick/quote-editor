<main class="container">
  <h1>Quoter</h1>
  <p class="mb-m">A blazing fast quote editor built with Hotwire</p>

  <% if user_signed_in? %>
    <%= link_to "View Quotes", quotes_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Login", new_user_session_path, class: "btn btn--dark" %>
  <% end %>
</main>
